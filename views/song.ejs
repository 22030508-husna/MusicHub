<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <title>MusicHub - Song Details</title>
</head>
<body>
    <nav class="navbar navbar-expand-sm bg-dark navbar-dark">
        <div class="container-fluid">
            <a class="navbar-brand" href="/">MusicHub</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#collapsibleNavbar">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="collapsibleNavbar">
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="nav-link" href="/">Home</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>
    <div class="container mt-4">
        <div class="text-center mb-4">
            <h2>Song Details</h2>
        </div>
        <% if (song) { %>
            <div class="row justify-content-center">
                <div class="col-md-6">
                    <div class="card">
                        <div class="card-body">
                            <p class="card-text"><b>Title:</b> <%= song.title %></p>
                            <p class="card-text"><b>Artist:</b> <%= song.artist %></p>
                            <p class="card-text"><b>Album:</b> <%= song.album %></p>
                            <p class="card-text"><b>Genre:</b> <%= song.genre %></p>
                            <p class="card-text"><b>Comment:</b> <%= song.comment %></p>
                        </div>
                    </div>
                </div>
            </div>
        <% } else { %>
            <div class="alert alert-warning text-center">
                <p>No song found.</p>
            </div>
        <% } %>
        <div class="mt-4 text-center">
            <a href="/" class="btn btn-secondary">Back</a>
        </div>
    </div>
</body>
</html>
